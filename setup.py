from setuptools import setup

setup(name='token-cutter',
      version='0.0.1',
      description='Slices text on tokenizer boundaries',
      author='Alan Tseng',
      packages=['token_cutter'],
      license='MIT',
      )
